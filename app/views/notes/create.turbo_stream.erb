<%= turbo_stream.prepend "notes", partial: "notes/note", locals: { note: @note } %>
<%= turbo_stream.replace "new_note", partial: "notes/form", locals: { job_application: @job_application, note: Note.new } %>
